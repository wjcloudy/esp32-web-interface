{
  "name": "esp32-web-interface-tests",
  "private": true,
  "description": "Dev tooling for the OpenInverter ESP32 web interface: lint + E2E tests (the shipped app has no build step)",
  "type": "module",
  "scripts": {
    "lint": "eslint",
    "check:gz": "python gzip_assets.py --check",
    "mock": "node tests/mock-esp/server.mjs",
    "test:e2e": "playwright test",
    "test": "npm run lint && npm run check:gz && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "eslint": "^9.29.0"
  }
}
